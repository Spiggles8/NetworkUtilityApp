<UserControl x:Class="NetworkUtilityApp.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="1000">
    <DockPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,4,0,4">
            <TextBlock Text="Output Log" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button x:Name="BtnClear" Content="Clear" Margin="12,0,0,0" Padding="8,2"/>
            <Button x:Name="BtnSave" Content="Save" Margin="24,0,0,0" Padding="8,2"/>
        </StackPanel>
        <ListBox x:Name="LogList"
                 FontFamily="Consolas"
                 FontSize="12"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" TextWrapping="NoWrap"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</UserControl>
