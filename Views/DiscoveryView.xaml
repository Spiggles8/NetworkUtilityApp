<UserControl x:Class="NetworkUtilityApp.Views.DiscoveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1000">
    <!-- Discovery tab: scan an IP range using the selected adapter and show active hosts -->
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <!-- Row 0: scan controls; Row 1: results grid -->
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Scan controls: choose adapter and enter start/end IPs, then run/cancel/save -->
        <GroupBox Header="IP Range Scan" Grid.Row="0" Margin="0,0,0,8">
            <Grid Margin="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <!-- Rows: adapter selector, start IP, end IP, actions, progress, counts/ETA -->
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Adapter selector: network interface used for scanning -->
                <TextBlock Text="Adapter:" VerticalAlignment="Center"/>
                <ComboBox x:Name="CboAdapter" Grid.Column="1" Height="28" Margin="8,0"/>

                <!-- Start IP input (first address in range) -->
                <TextBlock Text="Start IP:" Grid.Row="1" VerticalAlignment="Center"/>
                <TextBox x:Name="TxtStartIp" Grid.Row="1" Grid.Column="1" Height="28" Margin="8,0"/>

                <!-- End IP input (last address in range) -->
                <TextBlock Text="End IP:" Grid.Row="2" VerticalAlignment="Center"/>
                <TextBox x:Name="TxtEndIp" Grid.Row="2" Grid.Column="1" Height="28" Margin="8,0"/>

                <!-- Actions: run scan, cancel, save results to file -->
                <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="5" Margin="0,8,0,0">
                    <Button x:Name="BtnScan" Content="Scan Now" Padding="10,4"/>
                    <Button x:Name="BtnCancel" Content="Cancel" Padding="10,4" Margin="8,0,0,0"/>
                    <Button x:Name="BtnSave" Content="Save Results..." Padding="10,4" Margin="8,0,0,0"/>
                </StackPanel>

                <!-- Progress: bar and counts + ETA -->
                <ProgressBar x:Name="PrgScan" Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="5" Height="20" Margin="0,8,0,0"/>
                <StackPanel Orientation="Horizontal" Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="5" Margin="0,8,0,0">
                    <TextBlock x:Name="LblCounts" Text="Scanned: 0 / 0 | Active: 0"/>
                    <TextBlock Text="  ETA: " Margin="16,0,0,0"/>
                    <TextBlock x:Name="LblEta" Text="--:--:--"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Results: discovered hosts with DNS/MAC/vendor and status -->
        <DataGrid x:Name="DgvResults" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="IP Address" Binding="{Binding Ip}" MinWidth="140"/>
                <DataGridTextColumn Header="Host Name" Binding="{Binding Hostname}" MinWidth="180"/>
                <DataGridTextColumn Header="MAC Address" Binding="{Binding Mac}" MinWidth="160"/>
                <DataGridTextColumn Header="Manufacturer" Binding="{Binding Manufacturer}" MinWidth="160"/>
                <DataGridTextColumn Header="Latency (ms)" Binding="{Binding LatencyMs}" MinWidth="120"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" MinWidth="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
