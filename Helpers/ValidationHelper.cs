using System.Net;

namespace NetworkUtilityApp.Helpers
{
  /// <summary>
  /// IPv4 and network-related validation & parsing helpers (partial).
  /// Split into IPv4, MasksAndCidr, Parsing, and HighLevel partials.
  /// </summary>
  public static partial class ValidationHelper
  {
  }
}